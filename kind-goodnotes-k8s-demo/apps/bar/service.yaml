apiVersion: v1
kind: Service
metadata:
  name: bar
  namespace: apps
spec:
  selector: { app: http-echo, variant: bar }
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 5678
